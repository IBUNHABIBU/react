// styling

import { makeStyles } from '@material-ui/core/styles';

const myStyle = makeStyles((theme) => ({
    icon: {
        marginRight: '20px',
    },
    button: {
        marginTop: '20px'
    }
}))

export default myStyle;